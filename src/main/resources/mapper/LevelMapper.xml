<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ruangong.our_land.mapper.LevelMapper">

    <select id="getLevel" resultType="ruangong.our_land.model.Level">
        select * from `level` where l_id=#{l_id};
    </select>
</mapper>